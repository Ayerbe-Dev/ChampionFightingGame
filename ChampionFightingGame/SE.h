#pragma once

enum {
	COMMON_SE_START,
	COMMON_SE_MAX,
};

enum {
	ROY_SE_START,
	ROY_SE_MAX,
};

enum {
	ERIC_SE_START,
	ERIC_SE_MAX,
};

enum {
	LEON_SE_START,
	LEON_SE_MAX,
};

enum {
	CHAMELEON_SE_START,
	CHAMELEON_SE_MAX,
};

enum {
	ANGELICA_SE_START,
	ANGELICA_SE_MAX,
};

enum {
	NIGHTSABER_SE_START,
	NIGHTSABER_SE_MAX,
};

enum {
	SULLY_SE_START,
	SULLY_SE_MAX,
};

enum {
	PRIEST_SE_START,
	PRIEST_SE_MAX,
};

enum {
	AZIEL_SE_START,
	AZIEL_SE_MAX,
};

enum {
	BRUNO_SE_START,
	BRUNO_SE_MAX,
};

enum {
	TESSA_SE_START,
	TESSA_SE_MAX,
};

enum {
	ALEJANDRO_SE_START,
	ALEJANDRO_SE_MAX,
};

enum {
	NORMAN_SE_START,
	NORMAN_SE_MAX,
};

enum {
	ATLAS_SE_START,
	ATLAS_SE_MAX,
};

enum {
	JULIUS_SE_START,
	JULIUS_SE_MAX,
};

enum {
	RAMONA_SE_START,
	RAMONA_SE_MAX,
};

enum {
	ZYAIR_SE_START,
	ZYAIR_SE_MAX,
};

enum {
	VESUVIUS_SE_START,
	VESUVIUS_SE_MAX,
};